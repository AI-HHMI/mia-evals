# lmd_ssl_v1_zebrafish_instance -- the FIT (the arms were finetuned on it) split: zebrafish_fish2_quadcube1 only.
#
# The volume entry is the SAME entry as in ../../lmd_ssl_v1_neuron_instance/data/fit.yaml (a unit test
# asserts it stays byte-equal): this task reports the zebrafish pair on its own because its numbers must not be
# read beside the eight-volume task's (see mws.toml). The complementary split is test.yaml.

resolutions:
- - 8.0
  - 8.0
  - 8.0
patch_size:
- 256
- 256
- 256
output_axes: lcxyz
samples_per_epoch: 1000

volumes:
- name: zebrafish_fish2_quadcube1
  path: /groups/miaai/miaai/lmd-v0.0.1/data/em-zebrafish-fish2/crop-003_quadcube1_x23908_y18314_z0.zarr
  image_key: raw
  label_key: labels/proofread-cell-seg230927
  zarr_version: zarr3
  bounding_box:
  - - 0
    - 2000
  - - 0
    - 2000
  - - 0
    - 1364
  weight: 0.25
